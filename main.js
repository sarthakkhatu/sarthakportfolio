var typed = new Typed(".text", {
    strings: ["Frontend Developer", "Web Developer"],
    typeSpeed: 2, // Adjust the typing speed (higher value = slower)
    backSpeed: 100,
    backDelay: 1000,
    loop: true
});
